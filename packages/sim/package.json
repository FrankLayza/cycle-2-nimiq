{
  "name": "@snake/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure deterministic Competitive Snake simulation — single source of truth for client, server, and replay verification. Zero runtime deps.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
